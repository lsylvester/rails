== Running UJS tests

Run the tests in headless mode by running:

   rake test:ujs

To run the tests in a browser, start the Rails UJS server by running:

   rake ujs:server

Then, in a seperate tab run the Karma server

   karma start karma.config.js --no-single-run --browsers Chrome
